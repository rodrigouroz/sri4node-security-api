{
  "sriUser": "#{app_sri_user}",
  "sriPassword": "#{app_sri_pw}",
  "vskoApiHost": "#{VSKO_API_HOST + "." + VSKO_DOMAIN}",
  "accessToken": #{JSON.stringify(if accessToken then accessToken else {})}
}